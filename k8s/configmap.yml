apiVersion: v1
kind: ConfigMap
metadata:
  name: ecommerce-config
data:
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "mydatabase"
  DB_USER: "user"
  PORT: "4000"
  NODE_ENV: "production"
  BASE_URL: "http://localhost:4000"
